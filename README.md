# I2C — Idea to Code Framework

Переиспользуемый фреймворк для автономного создания проектной документации и кода с помощью AI-агентов.

```
Идея → PRD → ADR → RFC → Code
```

Claude Code выступает оркестратором. Каждый документ проходит через конвейер из 5 специализированных субагентов:

```
Supervisor (pre-flight) → Researcher → Architect → Critic → Writer → Supervisor (post-review)
```

---

## Содержание

1. [Установка](#установка)
2. [Подключение к проекту](#подключение-к-проекту)
3. [Полный цикл: от идеи до кода](#полный-цикл-от-идеи-до-кода)
4. [Команды](#команды)
5. [Работа с существующим проектом](#работа-с-существующим-проектом)
6. [Обрыв сессии и resume](#обрыв-сессии-и-resume)
7. [Обновление документов](#обновление-документов)
8. [Агенты](#агенты)
9. [Структура файлов проекта](#структура-файлов-проекта)
10. [Структура фреймворка](#структура-фреймворка)
11. [Принципы](#принципы)

---

## Установка

Один раз на машину:

```bash
git clone https://github.com/... ~/i2c-agent-framework
cd ~/i2c-agent-framework
./install.sh
```

`install.sh` делает две вещи:
- Создаёт symlinks всех `/i2c-*` команд в `~/.claude/commands/`
- Добавляет `@~/i2c-agent-framework/CLAUDE.md` в `~/.claude/CLAUDE.md`

После этого все `/i2c-*` команды доступны глобально в любом проекте. Фреймворк обновляется через `git pull` — symlinks подхватывают изменения автоматически.

Для удаления: `./uninstall.sh`

---

## Подключение к проекту

### Новый проект

Открой Claude Code в любой директории и выполни:

```
/i2c-setup /path/to/your/project
```

Оркестратор задаст вопросы о проекте (название, домен, цель, пользователь, стек, сроки) и создаст `.i2c/` с пустым `MEMORY.md`. Первый шаг — `/i2c-create-prd`.

### Существующий проект

```
/i2c-setup /path/to/existing/project
```

Выбери вариант `[2] Существующий`. Оркестратор запустит Researcher в режиме Discovery: он прочитает структуру проекта, зависимости, конфиги и существующую документацию, затем составит черновик `MEMORY.md` с уже принятыми решениями. Ты подтверждаешь или правишь черновик — после этого `MEMORY.md` заполнен и агенты не будут предлагать альтернативы тому что уже в продакшне. Первый шаг — `/i2c-status`.

`/i2c-setup` добавляет одну строку в `CLAUDE.md` проекта:

```markdown
@~/i2c-agent-framework/CLAUDE.md
```

Claude Code инлайнит `@`-импорты при старте — инструкции оркестратора попадают в контекст автоматически, без скриптов.

---

## Полный цикл: от идеи до кода

### Шаг 1 — PRD: что строим

```
/i2c-create-prd
```

Запускает конвейер: Supervisor → Researcher → Architect → Critic → Writer → Supervisor.

Researcher исследует проблему, пользователей и рынок. Architect формулирует Vision, MVP scope, ключевые workflow и метрики. Critic проверяет что MVP не слишком широк, пользователь конкретный, метрики измеримы. Writer собирает финальный PRD по шаблону.

Результат: `docs/PRD.md`

После создания PRD оркестратор обновляет `MEMORY.md` — целевой пользователь, scope, ограничения.

---

### Шаг 2 — ADR: ключевые архитектурные решения

Для каждого решения которое трудно отменить:

```
/i2c-create-adr выбор архитектурного стиля
/i2c-create-adr выбор базы данных
/i2c-create-adr стратегия аутентификации
```

Создавай ADR в порядке зависимостей: сначала архитектурный стиль, затем стек, затем хранение данных, затем специфические компоненты.

Конвейер: Supervisor → Architect → Critic → Writer → Supervisor. Architect рассматривает минимум 2 альтернативы с реальными трейдоффами в контексте твоего проекта. Critic проверяет что минусы выбранного варианта названы честно, допущения явны.

Результат: `docs/ADR-001-[slug].md`, `docs/ADR-002-[slug].md`, ...

Каждое принятое решение автоматически попадает в `MEMORY.md` — следующие ADR и RFC уже не могут его переоткрыть.

---

### Шаг 3 — RFC: детальная спека компонентов

```
/i2c-create-rfc core data model
/i2c-create-rfc auth service
/i2c-create-rfc document processing pipeline
```

Один RFC = один компонент. Создавай в порядке зависимостей: сначала фундаментальные компоненты (data model, auth), затем те что на них опираются.

Конвейер: Supervisor → Researcher → Architect → Critic → Writer → Supervisor. Architect пишет полный SQL DDL, API контракты, алгоритмы нетривиальной логики, error handling. Critic проверяет что каждый RFC содержит минимум 4 Acceptance Criteria: happy path, performance, error case, integration.

Результат: `docs/rfc/RFC-001-[slug].md`, ...

Supervisor на pre-flight проверяет граф зависимостей — нельзя создать RFC раньше ADR от которого он зависит.

Когда создано 3+ RFC, оркестратор предложит запустить `/i2c-check`.

---

### Шаг 4 — Code: реализация по RFC

```
/i2c-code-rfc 1
/i2c-code-rfc 2
```

Реализовывай RFC в том же порядке зависимостей что и создавал.

Конвейер:
1. **Architect [Planning]** — разбивает RFC на модули, определяет какие можно реализовывать параллельно
2. **Critic [Plan review]** — проверяет что все AC покрыты, параллельные модули не конфликтуют
3. **Writer** — финализирует Implementation Plan в `docs/impl/IMPL-[N]-[slug].md`
4. **Coding agents** — оркестратор спавнит субагентов параллельно для независимых модулей, последовательно для зависимых
5. **Critic [Verification]** — читает написанный код, сверяет каждый AC из RFC: PASS / FAIL / PARTIAL

Если Verification выявляет проблемы — оркестратор предлагает доработать конкретные модули или зафиксировать как tech debt.

Результат: код в проекте + `docs/impl/IMPL-[N]-[slug].md` + запись в JOURNAL.md

---

### Проверка консистентности

В любой момент:

```
/i2c-check
```

Запускает диагностику: все ли ADR покрыты RFC, не конфликтуют ли RFC между собой, актуален ли MEMORY.md, есть ли у каждого RFC Acceptance Criteria. Выводит отчёт: ✅ OK / ⚠️ предупреждение / ❌ критическая проблема.

---

### Текущее состояние

```
/i2c-status
```

Показывает что создано (с датами), что следующее, открытые вопросы из JOURNAL.md.

---

## Команды

| Команда | Аргументы | Что делает |
|---------|-----------|-----------|
| `/i2c-setup` | `/path/to/project` | Подключает фреймворк к проекту (новый или существующий) |
| `/i2c-create-prd` | — | Создаёт Product Requirements Document |
| `/i2c-create-adr` | `название решения` | Создаёт Architecture Decision Record |
| `/i2c-create-rfc` | `название компонента` | Создаёт RFC для компонента |
| `/i2c-code-rfc` | `N` | Реализует компонент по RFC-N (спавнит coding-агентов) |
| `/i2c-verify-rfc` | `N` | Проверяет существующую реализацию против AC из RFC-N |
| `/i2c-update-prd` | `описание изменений` | Обновляет PRD после пивота |
| `/i2c-update-adr` | `N описание` | Пересматривает ADR-N (с предупреждением о зависимых RFC) |
| `/i2c-resume` | — | Продолжает пайплайн прерванный обрывом сессии |
| `/i2c-check` | — | Проверяет консистентность всей документации |
| `/i2c-status` | — | Что создано, что следующее, открытые вопросы |

---

## Работа с существующим проектом

Если у тебя уже есть код и ты хочешь задокументировать принятые решения:

**1. Подключи фреймворк:**
```
/i2c-setup /path/to/project  → выбери [2] Существующий
```
Researcher проаудирует кодовую базу и заполнит MEMORY.md.

**2. Проверь что отсутствует:**
```
/i2c-status
/i2c-check
```

**3. Создай недостающие документы в нужном порядке:**
```
/i2c-create-prd           ← если нет формального PRD
/i2c-create-adr ...       ← для каждого ключевого решения
/i2c-create-rfc ...       ← для компонентов которые нужно документировать
```

**4. Верифицируй существующий код:**
```
/i2c-verify-rfc 1         ← проверит код против AC из RFC
```

---

## Обрыв сессии и resume

Если сессия Claude Code прервалась посередине пайплайна:

```
/i2c-resume
```

Оркестратор читает `.i2c/pipeline_state.json` и показывает:

```
⚠️ Незавершённый пайплайн: create-rfc auth-service
Завершённые шаги: supervisor-preflight, researcher, architect
Следующий шаг: critic

[1] resume  — продолжить с шага critic
[2] restart — начать заново
[3] abandon — отменить
```

Все промежуточные файлы в `.i2c/scratch/` сохраняются между сессиями — работа Researcher и Architect не потеряна.

---

## Обновление документов

### Пивот или изменение scope

```
/i2c-update-prd "меняем целевого пользователя с B2C на B2B"
```

Запускает сокращённый конвейер (без Researcher): Architect переписывает только изменившиеся секции, Critic проверяет что изменения не ломают ADR, Writer собирает новую версию. Supervisor проверяет консистентность с зависимыми документами.

### Пересмотр архитектурного решения

```
/i2c-update-adr 2 "PostgreSQL не справляется с нагрузкой, рассматриваем переход на Cassandra"
```

Перед стартом оркестратор предупредит о всех RFC которые зависят от этого ADR. После принятия нового решения — покажет список RFC для пересмотра.

---

## Агенты

| Агент | Режимы | Роль |
|-------|--------|------|
| **Supervisor** | Pre-flight, Post-review | Pre-flight: нужен ли артефакт? Post-review: вписывается в экосистему? |
| **Researcher** | PRD, RFC, Discovery | PRD: проблема, пользователи, рынок. RFC: контекст компонента, аналоги. Discovery: аудит существующего кода |
| **Architect** | PRD, ADR, RFC, Planning | Проектирует решение, взвешивает трейдоффы. Planning: разбивает RFC на модули для coding-агентов |
| **Critic** | PRD, ADR, RFC, Planning, Verification | Атакует черновики. Verification: проверяет реализацию против AC |
| **Writer** | Все документальные | Финализирует документ строго по шаблону |

Все агенты имеют лимиты длины: Researcher ≤ 400 слов, Architect ≤ 600–1000 слов, Critic ≤ 300 слов. Это не ограничение — это дисциплина: краткость означает что агент понял задачу.

---

## Структура файлов проекта

```
your-project/
  CLAUDE.md                        ← содержит @~/i2c-agent-framework/CLAUDE.md
  .i2c/
    config.md                      ← контекст проекта (заполняется при setup)
    MEMORY.md                      ← все принятые решения (единый источник истины)
    GOALS.md                       ← текущая стадия и следующий шаг
    JOURNAL.md                     ← лог: что создано, открытые вопросы, паттерны
    pipeline_state.json            ← стейт текущего пайплайна (для resume)
    scratch/                       ← временные файлы агентов (не коммитить)
      research.md
      prd-draft.md
      prd-review.md
      ...
  docs/
    PRD.md                         ← Product Requirements Document
    ADR-001-arch-style.md          ← Architecture Decision Records
    ADR-002-database.md
    rfc/
      RFC-001-data-model.md        ← Request for Comments
      RFC-002-auth-service.md
    impl/
      IMPL-001-data-model.md       ← Implementation Plans
      IMPL-002-auth-service.md
```

**Что коммитить:** всё кроме `.i2c/scratch/`

**`.gitignore`:**
```
.i2c/scratch/
```

---

## Структура фреймворка

```
~/i2c-agent-framework/
  CLAUDE.md                — инструкции оркестратора (полный playbook)
  WHO_AM_I.md              — identity и принципы фреймворка
  install.sh               — установка: symlinks + глобальная регистрация
  uninstall.sh             — удаление
  agents/
    supervisor.md          — промпт: pre-flight и post-review
    researcher.md          — промпт: PRD / RFC / Discovery режимы
    architect.md           — промпт: PRD / ADR / RFC / Planning режимы
    critic.md              — промпт: документы + Verification режим
    writer.md              — промпт: финализация по шаблону
  commands/                — slash команды → symlink в ~/.claude/commands/
    i2c-setup.md
    i2c-create-prd.md
    i2c-create-adr.md
    i2c-create-rfc.md
    i2c-code-rfc.md
    i2c-verify-rfc.md
    i2c-update-prd.md
    i2c-update-adr.md
    i2c-resume.md
    i2c-status.md
    i2c-check.md
  templates/
    PRD.md                 — шаблон Product Requirements Document
    ADR.md                 — шаблон Architecture Decision Record
    RFC.md                 — шаблон Request for Comments
    IMPL.md                — шаблон Implementation Plan
    MEMORY.md              — структурированный шаблон памяти проекта
  diagnostics/
    review-checklist.md    — чеклист консистентности для /i2c-check
```

---

## Принципы

**Документы создаются агентами, не людьми.** Человек задаёт цель и подтверждает результат. Агенты исследуют, проектируют, критикуют, пишут.

**MEMORY.md — закон.** Решения зафиксированные в MEMORY.md не переоткрываются. Следующий RFC не будет предлагать "рассмотреть PostgreSQL" если в MEMORY.md уже написано "выбрали MongoDB, ADR-002".

**Критика обязательна.** Ни один черновик не становится финальным без Critic. Это не опциональный шаг — это архитектурный инвариант.

**Стейт сохраняется.** `pipeline_state.json` фиксирует каждый шаг. Обрыв сессии — не потеря работы, а пауза.

**RFC — это контракт.** Документ считается готовым только если coding-агент может реализовать его без единого вопроса: DDL написан, API контракты определены, edge cases описаны, AC проверяемы.

**Supervisor — привратник.** Документ попадает в `docs/` только после ACCEPTED от Supervisor. Два отказа подряд — Human-in-the-loop с тремя вариантами: publish / retry / abandon.
