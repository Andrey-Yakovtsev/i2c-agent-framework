# RFC-[NNN]: [Название компонента]

**Статус:** Draft / Under Review / Accepted
**Версия:** 1.0
**Дата:** [дата]
**Автор:** I2C Framework
**Блокирует:** [RFC-NNN, RFC-NNN]
**Зависит от:** [RFC-NNN, RFC-NNN]

> **Для агентов:** Если интерфейс, поведение или ограничение не определено в RFC-входных данных — ставь `[NEEDS CLARIFICATION: <конкретный вопрос>]` прямо в тексте секции. Не угадывай. Максимум 3 маркера на документ.

---

## Резюме

[2-3 предложения: что этот компонент делает, какую роль играет в системе, какой принцип лежит в основе дизайна.]

---

## Мотивация

[Почему этот компонент нужен. Что происходит без него. Какую проблему он решает в контексте всей системы.]

---

## Детальный дизайн

### Структура / Архитектура

[Схема или описание внутреннего устройства компонента. Как устроен изнутри.]

```
[ASCII-схема или описание структуры]
```

### Интерфейсы

[Все публичные API, события, контракты. Конкретно.]

#### API Endpoints (если применимо)

```
POST /api/v1/[resource]
Request: { ... }
Response: { ... }
Errors: 400, 401, 404, 500
```

#### События (если применимо)

```
Event: [event.name]
Payload: { ... }
Producer: [кто публикует]
Consumer: [кто потребляет]
```

### Схема данных

```sql
-- Полный DDL с типами, constraints, индексами
CREATE TABLE ... (
  ...
);
```

### Основная логика

[Алгоритм / последовательность шагов. Можно псевдокодом или Python-сниппетами.]

```python
# Ключевые части реализации
```

### Обработка ошибок

| Ситуация | Поведение | Код / Event |
|----------|-----------|-------------|
| [Ошибка 1] | [что делаем] | [HTTP код или event] |
| [Ошибка 2] | ... | ... |

### Нефункциональные аспекты

**Производительность:**
- Целевые метрики: [latency p95, throughput]
- Как достигаем: [кэш, индексы, батчинг]

**Надёжность:**
- Retry стратегия: [N попыток, backoff]
- Fallback: [что делаем при недоступности]

**Масштабируемость:**
- Триггер масштабирования: [при каком условии]
- Как масштабируем: [горизонтально / вертикально / шардирование]

---

## Acceptance Criteria

Компонент считается реализованным когда:

- [ ] [Критерий 1]: при [условие] происходит [результат], проверяется через [как]
- [ ] [Критерий 2]: ...
- [ ] [Критерий 3 — error case]: при [ошибочное условие] возвращается [корректный ответ]
- [ ] [Критерий 4 — нефункциональный]: [метрика] не превышает [значение] при [нагрузке]

---

## Отклонённые альтернативы

### [Альтернатива 1]
[Краткое описание] — отклонено потому что [причина].

### [Альтернатива 2]
[Краткое описание] — отклонено потому что [причина].

---

## Нерешённые вопросы

- [ ] [Вопрос]: [почему важен] → [кто / как решает]

---

## Связанные RFC

- [RFC-NNN: название] — [связь]
