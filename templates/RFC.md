# RFC-[NNN]: [Название компонента]

**Статус:** Draft / Under Review / Accepted
**Версия:** 1.0
**Дата:** [дата]
**Автор:** I2C Framework
**Блокирует:** [RFC-NNN, RFC-NNN]
**Зависит от:** [RFC-NNN, RFC-NNN]

> **Для агентов:** Если интерфейс, поведение или ограничение не определено в RFC-входных данных — ставь `[NEEDS CLARIFICATION: <конкретный вопрос>]` прямо в тексте секции. Не угадывай. Максимум 3 маркера на документ.

---

## Резюме

[2-3 предложения: что этот компонент делает, какую роль играет в системе, какой принцип лежит в основе дизайна.]

---

## Мотивация

[Почему этот компонент нужен. Что происходит без него. Какую проблему он решает в контексте всей системы.]

---

## Детальный дизайн

### Структура / Архитектура

[Схема или описание внутреннего устройства компонента. Как устроен изнутри.]

```
[ASCII-схема или описание структуры]
```

### Интерфейсы

[Все публичные API, события, контракты. Конкретно.]

#### API Endpoints (если применимо)

```
POST /api/v1/[resource]
Request: { ... }
Response: { ... }
Errors: 400, 401, 404, 500
```

#### События (если применимо)

```
Event: [event.name]
Payload: { ... }
Producer: [кто публикует]
Consumer: [кто потребляет]
```

### Схема данных и миграция

```sql
-- Полный DDL с типами, constraints, индексами
CREATE TABLE ... (
  ...
);
```

**Тип изменения схемы:** additive / breaking / destructive
*(additive = новые поля с DEFAULT или nullable; breaking = изменение типов, переименование, удаление; destructive = DROP)*

**Миграция up:**
```sql
-- ALTER TABLE / CREATE INDEX / ...
```

**Миграция down (rollback):**
```sql
-- Обратный скрипт или "необратимо — точка невозврата"
```

**Нулевой простой:** да / нет — если нет, объясни почему и укажи требуемое maintenance window.

### Основная логика

[Алгоритм / последовательность шагов. Можно псевдокодом или Python-сниппетами.]

```python
# Ключевые части реализации
```

### Обработка ошибок

| Ситуация | Поведение | Код / Event |
|----------|-----------|-------------|
| [Ошибка 1] | [что делаем] | [HTTP код или event] |
| [Ошибка 2] | ... | ... |

### Нефункциональные аспекты

**Производительность:**
- Целевые метрики: [latency p95, throughput]
- Как достигаем: [кэш, индексы, батчинг]

**Надёжность:**
- Retry стратегия: [N попыток, backoff]
- Fallback: [что делаем при недоступности]

**Масштабируемость:**
- Триггер масштабирования: [при каком условии]
- Как масштабируем: [горизонтально / вертикально / шардирование]

---

### Наблюдаемость

| Уровень | Что логировать | Когда |
|---------|----------------|-------|
| INFO    | [событие]      | [условие] |
| WARN    | [событие]      | [условие] |
| ERROR   | [событие + контекст] | [условие] |

> Секреты, PII и токены — **никогда** не логировать.

**Метрики:** [что эмитировать, с какими labels/tags — например `component`, `status`]

**Трейсы:** trace_id обязателен / опционален — [обоснование]

**SLO:**
- Latency: p99 ≤ [X] ms при [Y] rps
- Error rate: ≤ [Z]%
- Доступность: [N]%

---

### Конфигурация и секреты

| Параметр | Тип | Обязателен | По умолчанию | Описание |
|----------|-----|------------|--------------|----------|
| [VAR_NAME] | env / config | да / нет | — | [описание] |

**Секреты** (не хранить в коде, не логировать):
- [список переменных которые содержат credentials, tokens, keys]

**Валидация при старте:** [что проверяется при инициализации компонента — fail-fast если отсутствует]

---

### Безопасность

**Поверхности атаки:**
- [endpoint / функция]: [что проверяется — аутентификация, авторизация, валидация входа]

**Данные пользователей:** есть / нет — если есть: [как хранятся, at-rest шифрование, кто имеет доступ]

**Внешние зависимости:** [список сервисов, которые вызывает компонент — как верифицируется их ответ]

> Если компонент обрабатывает пользовательский ввод — обязательно: валидация длины, типов, спецсимволов.

---

## Acceptance Criteria

Компонент считается реализованным когда:

- [ ] [Критерий 1]: при [условие] происходит [результат], проверяется через [как]
- [ ] [Критерий 2]: ...
- [ ] [Критерий 3 — error case]: при [ошибочное условие] возвращается [корректный ответ]
- [ ] [Критерий 4 — нефункциональный]: [метрика] не превышает [значение] при [нагрузке]

---

## Отклонённые альтернативы

### [Альтернатива 1]
[Краткое описание] — отклонено потому что [причина].

### [Альтернатива 2]
[Краткое описание] — отклонено потому что [причина].

---

## Нерешённые вопросы

- [ ] [Вопрос]: [почему важен] → [кто / как решает]

---

## Связанные RFC

- [RFC-NNN: название] — [связь]
